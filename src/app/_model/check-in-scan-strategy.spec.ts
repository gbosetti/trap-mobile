import { CheckInScanStrategy } from './check-in-scan-strategy';

describe('CheckInScanStrategy', () => {
  it('should create an instance', () => {
    expect(new CheckInScanStrategy()).toBeTruthy();
  });
});
