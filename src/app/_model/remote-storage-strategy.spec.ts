import { RemoteStorageStrategy } from './remote-storage-strategy';

describe('RemoteStorageStrategy', () => {
  it('should create an instance', () => {
    expect(new RemoteStorageStrategy()).toBeTruthy();
  });
});
