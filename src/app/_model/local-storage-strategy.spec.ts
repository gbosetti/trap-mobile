import { LocalStorageStrategy } from './local-storage-strategy';

describe('LocalStorageStrategy', () => {
  it('should create an instance', () => {
    expect(new LocalStorageStrategy()).toBeTruthy();
  });
});
